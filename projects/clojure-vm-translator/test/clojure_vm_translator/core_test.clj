(ns clojure-vm-translator.core-test
  (:require [clojure.test :refer :all]
            [clojure-vm-translator.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
